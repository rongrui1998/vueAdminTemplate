import { Document, Grid, House, Lock, Menu, Setting, Tickets } from '@element-plus/icons-vue';
import type { Component } from 'vue';

export const appIconMap: Record<string, Component> = {
  Document,
  Grid,
  House,
  Lock,
  Menu,
  Setting,
  Tickets,
};
