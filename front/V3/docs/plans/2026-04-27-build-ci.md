# Build Warning Cleanup And CI Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove the remaining `front/V3` build warnings and add a minimal CI workflow that automatically runs the project quality gates.

**Architecture:** Simplify the bundle strategy instead of further deep-splitting Element Plus internals, and remove unnecessary icon bulk registration so vendor composition better matches the Base template footprint. Add a dedicated GitHub Actions workflow scoped to `front/V3`.

**Tech Stack:** Vue 3, Vite, Element Plus, GitHub Actions, pnpm

---

### Task 1: Remove unnecessary global icon bulk registration

**Files:**

- Create: `front/V3/src/constants/icons.ts`
- Modify: `front/V3/src/components/AppIcon/index.vue`
- Modify: `front/V3/src/plugins/element-plus.ts`

**Step 1:** Add an explicit icon map for only the menu icons used by Base.
**Step 2:** Refactor `AppIcon` to resolve icons from the local map instead of global app registration.
**Step 3:** Remove bulk icon registration from the Element Plus bootstrap file.
**Step 4:** Re-run lint/tests.

### Task 2: Stabilize build chunking

**Files:**

- Modify: `front/V3/vite.config.ts`

**Step 1:** Replace the current experimental Element Plus sub-chunk split with stable high-level vendor groups.
**Step 2:** Add a clear `chunkSizeWarningLimit` suitable for the Base template footprint.
**Step 3:** Rebuild and verify the warning output is gone.

### Task 3: Add GitHub Actions CI

**Files:**

- Create: `.github/workflows/front-v3-ci.yml`
- Modify: `front/V3/README.md`

**Step 1:** Add a workflow scoped to `front/V3` changes and the workflow file itself.
**Step 2:** Configure Node + pnpm setup and dependency caching.
**Step 3:** Run lint, stylelint, test, and build in sequence.
**Step 4:** Mention the CI gate in README.

### Task 4: Full verification

**Files:**

- Verify only

**Step 1:** Run `pnpm lint`.
**Step 2:** Run `pnpm stylelint`.
**Step 3:** Run `pnpm test`.
**Step 4:** Run `pnpm build`.
**Step 5:** Confirm build output no longer emits the previous chunk warnings.
